# How to make docs

Add to docs by making new notebook or markdown pages and storing them in this directory. For notebooks, you can prerun them so they look correct.

Add them by name to `index.rst`.

Make docs locally with

    make html

Look at files starting from `_build/html/index.html`.